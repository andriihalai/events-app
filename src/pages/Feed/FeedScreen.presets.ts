import {StyleSheet, ViewStyle} from 'react-native';
import styles from '../screen.layout.presets';

const eventsListWrapper: ViewStyle = {
  flex: 1,
};

export default StyleSheet.create({
  eventsListWrapper,
  ...styles,
});
